

## تحديث تقارير الموارد البشرية وإصلاح الأخطاء

### المشكلات المكتشفة

1. **خطأ 400 في عدة استعلامات**: الاستعلامات تطلب عمود `identity_number` من جدول `hr_employees` لكنه غير موجود — الاسم الصحيح هو `national_id`
   - يؤثر على: تقرير الإجازات، أرصدة الإجازات، السلف (3 استعلامات معطلة)

2. **تقرير الخصومات غير موجود**: لا يوجد تبويب للخصومات/الجزاءات في شاشة التقارير رغم وجود بيانات `hr_deductions`

### التعديلات المطلوبة في `src/pages/client/hr/HRReports.tsx`

#### 1. إصلاح اسم العمود
تغيير `identity_number` إلى `national_id` في 3 استعلامات:
- `hr_leaves` select → `hr_employees(name, name_en, employee_number, national_id)`
- `hr_loans` select → نفس التغيير
- `hr_leave_balances` select → نفس التغيير
- دالة `matchEmployee` → استخدام `national_id` بدل `identity_number`

#### 2. إضافة تبويب تقرير الخصومات
- إضافة استعلام جديد لجلب `hr_deductions` مع بيانات الموظف و قاعدة الجزاء
- فلاتر: بحث + تاريخ من/إلى
- بطاقات ملخص: إجمالي الخصومات، عدد الخصومات المطبقة، عدد المعلقة
- جدول يعرض: الموظف، نوع المخالفة، المبلغ، الشهر المستهدف، الحالة
- تصدير Excel
- تغيير `grid-cols-5` إلى `grid-cols-6` في TabsList

#### 3. الملفات المتأثرة
- `src/pages/client/hr/HRReports.tsx` فقط

