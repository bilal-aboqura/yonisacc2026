

## إصلاح زر الاعتماد — المشكلة والحل

### المشكلة
نافذة التأكيد (`AlertDialog`) موجودة في كود القائمة الرئيسية (سطر 1084) لكن زر "اعتماد" يُضغط من شاشة التفاصيل (`viewRun`) التي تعمل `return` مبكراً في سطر 952. لذلك النافذة لا تُعرض أبداً عند الضغط على الزر.

### الحل
نقل `AlertDialog` من نهاية الملف (سطر 1084-1119) إلى داخل شاشة التفاصيل (`viewRun`) قبل `return` الإغلاق في سطر 951-952.

### الملف المتأثر
- `src/pages/client/hr/Payroll.tsx` — نقل AlertDialog إلى داخل block عرض التفاصيل

