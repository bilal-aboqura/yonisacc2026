

# حذف شاشة الفترات المالية المكررة

## التحليل
شاشة "الفترات المالية" (`FiscalPeriods.tsx`) في الإعدادات أصبحت مكررة بعد بناء شاشة "إدارة السنوات المالية" (`FiscalYearManagement.tsx`) التي تحتوي على كل وظائفها وأكثر.

## الحالة الحالية للإقفال السنوي
جميع المراحل الأربع مكتملة ✅ (البنية التحتية، الواجهة، RPCs، التقارير).

## التغييرات المطلوبة

### 1. حذف الملف `src/pages/client/FiscalPeriods.tsx`

### 2. تعديل `src/App.tsx`
- حذف import لـ `FiscalPeriods`
- حذف المسار `settings/fiscal-periods`

### 3. تعديل `src/components/client/ClientLayout.tsx`
- حذف رابط "الفترات المالية" من مجموعة الإعدادات (سطر 230)

